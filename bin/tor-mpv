#!/bin/sh

while :
do
    torsocks -i mpv "$@" && {
        printf 'Press Return to exit ... '
        A=$(timeout --foreground 0.5 cat | sed 's/^$/a/')
        [ -n "$A" ] \
            && exit 0
    }
done
