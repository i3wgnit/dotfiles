#!/bin/sh

STATE="$(pactl list sources | \
    sed -n '1,/^[[:space:]]Name: alsa_input/d;/^[[:space:]]Mute/p;/^[[:space:]]Mute/q' | \
    grep -o 'y')"

if [ -n "$STATE" ]; then
    echo Mic
else
    echo MIC
fi
